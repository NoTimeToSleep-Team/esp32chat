# Common Platform Module

Platform abstraction points used by firmware targets.

Planned responsibilities:

- monotonic time provider abstraction;
- storage and key-value abstraction;
- logging adapter per platform;
- network status abstraction.
