# Common Messages Mapping

Reference mapping between `contracts/messages/*.json` and firmware-internal message structs.

`v0.07.01` keeps this as a placeholder to avoid schema drift in later code stages.
